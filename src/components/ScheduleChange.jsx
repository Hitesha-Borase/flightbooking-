import React, { useState } from 'react'; import Button from '@mui/material/Button'; import Dialog from '@mui/material/Dialog'; import DialogActions from '@mui/material/DialogActions'; import DialogContent from '@mui/material/DialogContent'; import DialogTitle from '@mui/material/DialogTitle'; import TextField from '@mui/material/TextField'; import Typography from '@mui/material/Typography';
export default function ScheduleChange({ event, onClose, onSave }) { const [note, setNote] = useState(''); return <Dialog open={Boolean(event)} onClose={onClose} fullWidth maxWidth="sm"><DialogTitle>PNR {event?.pnr} — Schedule Change Alert</DialogTitle><DialogContent><Typography variant="body2">Original: AA 100 | 15OCT | 18:30 → 07:30+1<br />New: AA 100 | 16OCT | 09:00 → 22:00</Typography><TextField fullWidth multiline rows={3} label="Internal Note" value={note} onChange={e => setNote(e.target.value)} sx={{ mt: 2 }} /></DialogContent><DialogActions>{['Accept Change — Notify Customer','Request Reschedule','Request Refund','Contact Airline Directly'].map(x => <Button key={x} onClick={() => onSave(x, note)}>{x}</Button>)}<Button variant="contained" onClick={onClose}>Save & Notify</Button></DialogActions></Dialog>; }
