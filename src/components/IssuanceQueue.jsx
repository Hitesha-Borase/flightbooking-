import React from 'react'; import Box from '@mui/material/Box'; import Button from '@mui/material/Button'; import Chip from '@mui/material/Chip'; import Paper from '@mui/material/Paper'; import Typography from '@mui/material/Typography';
const items = [{id:'TK-451',name:'M. Chen',route:'JFK → LHR',pnr:'ABC12D',amount:'$200.00'},{id:'TK-452',name:'A. Lee',route:'DEL → SIN',pnr:'LMN78F',amount:'$530.00'}];
export default function IssuanceQueue({ onSelect }) { return <Paper elevation={0} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2.5 }}><Box sx={{ display: 'flex', justifyContent: 'space-between', mb: 1 }}><Typography variant="subtitle2" sx={{ fontWeight: 900 }}>READY-FOR-ISSUANCE QUEUE</Typography><Chip label="12 Leads Ready" size="small" color="success" /></Box><Typography variant="caption" color="text.secondary">Paid leads are awaiting ticket generation.</Typography>{items.map(item => <Paper key={item.id} variant="outlined" sx={{ p: 1.5, mt: 1.5 }}><b>Lead #{item.id} — {item.name}</b><br />✈ {item.route}<br />✅ Payment Confirmed ({item.amount})<br />◉ Sabre PNR: {item.pnr}<Box sx={{ mt: 1 }}><Button size="small" variant="contained" onClick={() => onSelect(item)}>Issue E-Ticket</Button><Button size="small" onClick={() => onSelect(item)}>Review Details</Button></Box></Paper>)}</Paper>; }
