import React, { useState } from 'react'; import Button from '@mui/material/Button'; import Paper from '@mui/material/Paper'; import TextField from '@mui/material/TextField'; import Typography from '@mui/material/Typography';
export default function TicketControls({ item, onIssue }) { const [numbers, setNumbers] = useState('0172345678901\n0172345678902'); return <Paper elevation={0} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2.5 }}><Typography variant="subtitle2" sx={{ fontWeight: 900 }}>TICKET ISSUANCE CONTROLS</Typography><Typography variant="body2" color="text.secondary" sx={{ my: 1 }}>Enter the 13-digit e-ticket codes from your GDS notes.</Typography><TextField fullWidth label="Enter PNR" value={item?.pnr || ''} InputProps={{ readOnly: true }} size="small" /><TextField fullWidth multiline rows={4} label="Enter 13-Digit E-Ticket Number(s)" helperText="One per line, per passenger" value={numbers} onChange={e => setNumbers(e.target.value)} sx={{ mt: 2 }} /><Button fullWidth variant="contained" color="success" sx={{ mt: 2 }} disabled={!item} onClick={() => onIssue(numbers)}>Submit & Notify Client</Button></Paper>; }
