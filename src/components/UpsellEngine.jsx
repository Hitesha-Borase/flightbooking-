import React from 'react'; import Paper from '@mui/material/Paper'; import Typography from '@mui/material/Typography';
export default function UpsellEngine() { return <Paper elevation={0} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2.5 }}><Typography variant="subtitle2" sx={{ fontWeight: 900, mb: 1 }}>UPSELL SUGGESTIONS ENGINE</Typography>{['Premium seat selection — extra legroom / exit row','Extra baggage — 23kg or 32kg add-on','Travel insurance — recommended for booking value','Partner hotel, airport transfer, and priority check-in'].map(x => <Typography key={x} variant="body2" sx={{ mb: .6 }}>• {x}</Typography>)}</Paper>; }
