import React, { useState } from 'react'; import Box from '@mui/material/Box'; import Button from '@mui/material/Button'; import Chip from '@mui/material/Chip'; import Paper from '@mui/material/Paper'; import Typography from '@mui/material/Typography';
const feed = [{pnr:'XYZ34E',name:'J. Smith',text:'Schedule Change Alert — Reschedule Needed',color:'error'},{pnr:'LMN78F',name:'A. Lee',text:'Ticketed & Dispatched — PDF Itinerary Sent',color:'success'},{pnr:'QRS90G',name:'S. Williams',text:'Flight Delay — Delayed 2 Hours',color:'warning'},{pnr:'QRS38F',name:'J. Smith',text:'Ticketed & Dispatched',color:'success'}];
export default function PNRTracker({ onAction }) { return <Paper elevation={0} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2.5 }}><Typography variant="subtitle2" sx={{ fontWeight: 900 }}>FLIGHT PNR AUTO-TRACKER <Chip label="LIVE" color="success" size="small" /></Typography>{feed.map(x => <Box key={x.pnr} sx={{ py: 1.3, borderBottom: '1px solid', borderColor: 'divider' }}><Typography variant="body2" sx={{ fontWeight: 800 }}><Chip color={x.color} size="small" label={x.pnr} /> {x.name}</Typography><Typography variant="caption">{x.text}</Typography><br />{x.color !== 'success' ? <Button size="small" onClick={() => onAction(x)}>Process Schedule Change</Button> : <Button size="small" onClick={() => onAction(x)}>Dispatch Official Itinerary</Button>}</Box>)}</Paper>; }
