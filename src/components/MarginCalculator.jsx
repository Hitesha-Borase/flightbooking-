import React, { useState } from 'react';
import Box from '@mui/material/Box'; import Button from '@mui/material/Button'; import Paper from '@mui/material/Paper'; import TextField from '@mui/material/TextField'; import Typography from '@mui/material/Typography';
export default function MarginCalculator({ onPublish }) { const [net, setNet] = useState(4500); const [markup, setMarkup] = useState(15); const [fixed, setFixed] = useState(0); const sell = Number(fixed) ? Number(net) + Number(fixed) : Number(net) * (1 + Number(markup) / 100); const profit = sell - Number(net); return <Paper elevation={0} sx={{ p: 2, border: '1px solid', borderColor: 'divider', borderRadius: 2.5 }}><Typography variant="subtitle2" sx={{ fontWeight: 900 }}>NET vs. SELLING PRICE CALCULATOR</Typography><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', sm: '1fr 1fr' }, gap: 2, mt: 1.5 }}><Box sx={{ fontSize: 13, p: 1, bgcolor: 'background.neutral', borderRadius: 1.5 }}>Parsed flights<br /><b>AA 100 J · 15OCT · JFK → LHR · 18:30 → 07:30+1</b></Box><Box sx={{ display: 'grid', gap: 1 }}><TextField label="Net fare (cost) USD" type="number" size="small" value={net} onChange={e => setNet(e.target.value)} /><TextField label="Manual markup %" type="number" size="small" value={markup} onChange={e => { setMarkup(e.target.value); setFixed(0); }} /><TextField label="Fixed markup USD" type="number" size="small" value={fixed} onChange={e => setFixed(e.target.value)} /><Typography>Selling Price: <b>USD {sell.toFixed(2)}</b></Typography><Typography color="success.main">NET PROFIT MARGIN: <b>USD {profit.toFixed(2)}</b></Typography></Box></Box><Button variant="contained" fullWidth sx={{ mt: 2 }} onClick={() => onPublish({ net, sell, profit })}>Generate & Publish Quote to Client Profile</Button></Paper>; }
